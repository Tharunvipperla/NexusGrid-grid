fastapi>=0.138.0
uvicorn[standard]>=0.42.0
sqlalchemy>=2.0.48
aiosqlite>=0.22.0
httpx>=0.28.0
cryptography>=48.0.1
argon2-cffi>=23.1.0
websockets>=16.0
psutil>=7.2.0
pydantic>=2.12.0
pyinstaller>=6.0.0
# Security floors (pre-launch audit 2026-06-21): transitive deps pinned above
# their last known-CVE versions.
starlette>=1.3.1
python-multipart>=0.0.31

# Optional — only needed for the GDrive cloud-eviction tier.
# The rest of the app starts and runs without these; the GDrive driver
# imports them lazily and only when a credential is actually loaded.
# google-api-python-client>=2.150.0
# google-auth>=2.40.0
