{
  "name": "nexus-webui",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Nexus P2P Grid — v3 management UI (React, bundled with esbuild).",
  "scripts": {
    "build": "esbuild src/app.jsx --bundle --minify --format=iife --target=es2018 --outfile=dist/bundle.js",
    "test": "node --test"
  },
  "dependencies": {
    "jszip": "3.10.1",
    "react": "18.3.1",
    "react-dom": "18.3.1"
  },
  "devDependencies": {
    "esbuild": "0.24.2"
  }
}
